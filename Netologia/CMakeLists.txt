cmake_minimum_required(VERSION 3.10)  # Минимальная версия CMake
project(Netologia)                    # Название проекта

# Добавляем исполняемый файл (укажи свои .cpp файлы!)
add_executable(Netologia FileName.cpp)